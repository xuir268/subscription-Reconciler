global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: subscription-reconciler
    metrics_path: /metrics
    static_configs:
      - targets:
          - service:8080
